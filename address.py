math=50
phy=50 # if that value already exits then compiler will assign the same memory address to it
eng=40
print(id(math)) # id() used to return address of variable
print(id(phy)) 
print(id(eng)) 